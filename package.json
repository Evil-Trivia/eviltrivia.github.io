{
  "scripts": {
    "deploy:database": "firebase deploy --only database",
    "deploy:functions-and-hosting": "firebase deploy --only functions:patreonAuth,hosting",
    "deploy:firebase": "firebase deploy --only database && firebase deploy --only functions:patreonAuth,hosting"
  },
  "devDependencies": {
    "firebase-tools": "^15.18.0"
  },
  "dependencies": {
    "firebase-admin": "^13.10.0",
    "tmp": "^0.2.3"
  },
  "overrides": {
    "uuid": "^11.1.1"
  }
}
